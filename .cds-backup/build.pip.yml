version: v1.0
name: build-pipeline
jobs:
- job: First job
  steps:
  - checkout: '{{.cds.workspace}}'
  - script: mvn package
  - artifactUpload:
      path: '{{.cds.workspace}}target/*.jar'
  requirements:
  - binary: git
